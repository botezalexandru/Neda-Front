export interface District {
  id: number;
  objectid: number;
  zone: string;
  district: string;
  population: number;
  pga_value: string;
  severity_class: string;
  dist_id: number;
  reg_code: string;
  zone_code: string;
  ocha_pcode: string;
  hlcit_code: string;
  first_dist: string;
  min_paramv: string;
  first_class: string;
  shape_leng: string;
  shape_area: string;
}
